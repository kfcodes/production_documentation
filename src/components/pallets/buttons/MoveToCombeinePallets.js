import Button from "@mui/material/Button";

export default function CombinePalletsButton() {
  return (
    <>
      <Button
        href="/combine_pallets"
        variant="contained"
        size="large"
        color="success"
      >
        Combine Pallets
      </Button>
    </>
  );
}
